= simple_form_for temple, :html => { :class => 'form-horizontal' } do |f|
  = f.error_notification

  = f.input :name, :input_html => { :class => 'span5' }

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to "Cancel", temples_path, :class => 'btn'


